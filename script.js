// Main script.js

document.addEventListener('DOMContentLoaded', function() {
    console.log("Main website script loaded.");

    // Example: Smooth scrolling for anchor links (if you add any)
    // document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    //     anchor.addEventListener('click', function (e) {
    //         e.preventDefault();
    //         document.querySelector(this.getAttribute('href')).scrollIntoView({
    //             behavior: 'smooth'
    //         });
    //     });
    // });

    // You could add functions here that are used across multiple pages,
    // or general event listeners for the entire site.
});